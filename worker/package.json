{
  "name": "critkey-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for CritKey Pro",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler dev --test"
  },
  "keywords": ["cloudflare", "worker", "canvas", "cors-proxy"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.1"
  }
}
