{
  "name": "critkey",
  "version": "1.1.1",
  "description": "CritKey - Fast rubric grading app with keyboard shortcuts",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix rubric-grader && npm install --prefix worker",
    "build": "cd rubric-grader && npm ci && npm run build",
    "dev": "echo '\n========================================' && echo '⚠️  REMINDER: Start the Cloudflare Worker!' && echo '========================================' && echo 'In a separate terminal, run:' && echo '  cd worker' && echo '  npm run dev' && echo '========================================\n' && cd rubric-grader && npm run dev",
    "preview": "cd rubric-grader && npm run preview",
    "worker:dev": "cd worker && npm run dev",
    "worker:deploy": "cd worker && npm run deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Chokichi/CritKey.git"
  },
  "keywords": [
    "rubric",
    "grading",
    "canvas",
    "education"
  ],
  "author": "",
  "license": "MIT"
}

